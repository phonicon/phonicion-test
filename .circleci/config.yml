version: 2.1
jobs:
  build:
    steps:
      - checkout
      - run: bash ./build.sh
  test:
    steps:
      - checkout
      - run: bash ./test.sh
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test