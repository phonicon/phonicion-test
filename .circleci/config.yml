version: 2.1
jobs:
  docker:
      - image: circleci/node:11.13.0-jessie-slim
  build:
    steps:
      - checkout
      - run: bash ./build.sh
  test:
    steps:
      - checkout
      - run: bash ./test.sh
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test